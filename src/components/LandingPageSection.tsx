import { HeroSection } from "./hero-section";

export const LandingPageSection = () => {
  return (
    <main className="flex flex-col bg-hero-gradient section-padding-x section-padding-y">
      <HeroSection />
    </main>
  );
};
