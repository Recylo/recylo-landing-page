import { HeroSection } from "./hero-section";

export const LandingPageSection = () => {
  return (
    <main className="flex flex-col section-padding-x section-padding-y">
      <HeroSection />
    </main>
  );
};
